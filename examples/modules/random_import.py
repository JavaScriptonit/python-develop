import random  # Импортируем стандартный модуль random

result = random.randint(1, 100)  # Генерируем случайное целое число в диапазоне от 1 до 100 и сохраняем его в переменную result

print(result)  # Выводим сгенерированное случайное число

result2 = dir(random)  # Получаем список всех атрибутов (методов, функций, переменных) модуля random и сохраняем его в переменную result2

print(result2)  # Выводим список всех атрибутов модуля random
